{% extends 'IasGameBundle::base.html.twig' %}

{% block page_stylesheets %}
    {{ parent() }}
    <link href="{{ asset('/bundles/iasgame/css/GameSession.css') }}" rel="stylesheet"/>
{% endblock %}

{% block body %}
    <div class="container">

        <div class="row">
            <div class="col-sm-2"></div>
            <div class="col-sm-8">

                {% for item in game_session %}

                    <h3>{{ item.name }}</h3>

                    <table class="table table-striped game_session">
                        {% for game in item.getGameSession() %}
                            <tr>
                                {#<td>{{ game.getGamer().getLogin }}</td>#}
                                <td>{{ game.created|date('Y-m-d H:i:s') }}</td>
                                <td>
                                    <a role="button" href="#{{ game.id }}" class="btn btn-info">Присоединиться</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </table>

                {% endfor %}

            </div>
            <div class="col-sm-2"></div>
        </div>
    </div>
{% endblock %}
